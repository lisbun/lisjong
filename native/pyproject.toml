[build-system]
requires = ["maturin>=1.15,<2"]
build-backend = "maturin"

[project]
name = "lisjong-native"
version = "0.1.0"
description = "Opt-in native backend prototype for lisjong hand evaluation (Issue #213)"
requires-python = ">=3.14,<3.15"
license = "MIT"
authors = [{ name = "lisbun" }]

[tool.maturin]
module-name = "_lisjong_native"
